[phases.install]
cmds = ["pip install -r requirements-server.txt"]

[phases.build]
cmds = ["python ingest.py"]

[phases.build.cache]
paths = ["/root/.cache/huggingface", "/root/.cache/torch"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
